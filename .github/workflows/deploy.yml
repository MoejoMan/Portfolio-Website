name: Deploy to PythonAnywhere

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Deploy to PythonAnywhere
        env:
          PYTHONANYWHERE_API_TOKEN: ${{ secrets.PA_API_TOKEN }}
        run: |
          # Install PythonAnywhere CLI for the current user
          pip install --upgrade --force-reinstall pythonanywhere --user
          
          # Add the CLI to PATH in this shell
          export PATH=$HOME/.local/bin:$PATH
          
          # Login and deploy
          pythonanywhere login --apikey $PYTHONANYWHERE_API_TOKEN
          pythonanywhere run "cd ~/mysite && git pull"
          pythonanywhere webapp reload Moejoe06.pythonanywhere.com
